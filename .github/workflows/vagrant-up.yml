name: vagrant-up

on: [push]

jobs:
  build:
    runs-on: macos-10.15

    steps:
    - uses: actions/checkout@v2

    - name: Run vagrant up and ssh into box after boot
      run: |
        vagrant --version
        vagrant up
        vagrant ssh -c "echo 'hello world!'"
